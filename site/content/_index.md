+++
# The landing page's meta description is `description` in config.toml — this page *is* the site
# root, so the two would be the same string in two places. A `description` here would be silently
# ignored, which is why there is not one.
template = "index.html"
+++
